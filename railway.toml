[build]
builder = "NIXPACKS"
buildCommand = "rm -rf node_modules && npm install --no-audit --no-fund && npx prisma generate && npm run build"

[deploy]
startCommand = "sh scripts/boot.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
